using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RandmColors : MonoBehaviour
{
    public Color[] RandomColor1, RandomColor2;//Set both numbers according to (Total Color parts) 
    public static RandmColors ins;
    void Start()
    {
        ins = this;
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
